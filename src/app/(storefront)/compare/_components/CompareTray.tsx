"use client";
import React from "react";

export default function CompareTray({ compareIds, onClear }: { compareIds?: string[], onClear?: () => void }) {
  return (
    <div className="fixed bottom-0 left-0 w-full bg-white shadow-[0_-4px_6px_-1px_rgba(0,0,0,0.1)] z-50 transform transition-transform duration-300 translate-y-0">
      <div className="container mx-auto px-4 py-4">
        {/* Placeholder for CompareTray functionality */}
      </div>
    </div>
  );
}
